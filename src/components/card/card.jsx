import React from 'react';
import styles from './card.module.css';

const Card = ({card}) => {
    //카드프리뷰 최종애들
    return(
        <div>
            <p>card.title</p>
            <p>card.content</p>
        </div>

    );
};

export default Card;